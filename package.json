{
  "name": "love-api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Medical Committee API - Backend Serverless Functions",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:queue-contract": "node scripts/check-queue-contract.js"
  }
}
